import { Button } from '@material-ui/core';

const ContactButton = () => {
    return(
        <div>
            <Button>
                Contact Me
            </Button>
        </div>
    )
}

export default ContactButton;